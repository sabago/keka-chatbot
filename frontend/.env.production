# Production Environment Configuration
# When deployed to Railway, the backend serves the frontend from the same domain
# So we use an empty string to make API calls to the same origin (relative URLs)
VITE_API_URL=
